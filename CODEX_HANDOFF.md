# Codex Handoff

## Repository

- Root: `C:\Users\donny\Desktop\US_stocks_swing_model_v2`
- Legacy root: `C:\Users\donny\Desktop\US_stocks_swing_model` (read-only)
- Branch: `remediate/project-assessment-seven-findings`
- State: cumulative assessment remediations are unstaged and uncommitted

## Authorized scope

Build through `REBUILD_COMPLETE` and `HISTORICAL_RESEARCH_READY`, but pause
before provider purchases, real-history hypothesis/WFA execution, candidate
sealing, destructive cutover, or external push.

## Current milestone

Status: `HISTORICAL_RESEARCH_READY_MECHANICAL_DISCOVERY_ONLY`.

- The approved migration hash-copied 4,911 files / 345,845,816 bytes into the
  content-addressed v2 vault. It did not move, link, or modify legacy files.
- The authoritative migration plan is
  `479e3943b0eeae69d08aa078eece05ff73b20def0500b13f746646bb1534ef82`.
- The complete non-active aggregate foundation release is
  `stock_historical_foundation_set/22d6c19ce3f7be6779f4763ad99129ea571b31b021cf9347fc4e7e74eaac4c66`.
- A bounded public Nasdaq snapshot is preserved as acquisition evidence only.
  Its self-hashed capability is not independently authenticated and is no
  longer trust-eligible. Alpaca remains unqualified because credentials were
  unavailable to this process; no bulk Alpaca backfill was attempted.
- Detached network-acquisition attestation verification is implemented.
  Network capture now emits an exact signing request but makes no qualification
  claim; only an externally signed, registry-bound receipt can be verified
  offline and promote the matching immutable snapshot.
- Provider entrypoints now require a separate, single-use externally signed
  request authorization before network access. The receipt binds the exact
  initial URL, registry, limits, pagination family, expiry, and nonce; an
  interrupted use is spent rather than replayed.
- The historical-foundation CLI is plan-only. Mutating foundation mechanics
  are synthetic-fixture-only and root-bound; there is no production
  publication authority.
- Direct HFDL and failed-Alpaca qualification publication boundaries now
  require exact synthetic action permits and containing fixture roots before
  any filesystem mutation.
- Asymmetric external-authorization verification is implemented for pinned
  RSA public JWKs. The authority registry remains `NOT_CONFIGURED`, so no
  external authority is active; shared-secret receipts grant no authority,
  production private keys remain outside the repository, and the completed
  controlled-rebuild receipt cannot be reused.
- No real-history hypothesis, model fit, WFA, candidate, or alpha evaluation
  ran.
- Non-authorizing `REBUILD_COMPLETE` and mechanical
  `HISTORICAL_RESEARCH_READY` receipts are published in the accepted-data
  vault and bind a clean local commit. They do not clear any trust blocker.
- Robustness policy and evidence hashes now bind sleeve, book, evaluation, and
  bundle artifacts. Definite failures take precedence; underpowered robustness
  evidence remains explicitly inconclusive.
- Prospective monitoring mechanics are append-only and bundle/policy/reference
  bound. Paused or invalid monitoring abstains and cannot resume without an
  exact signed review; monitoring cannot retrain, retune, substitute sources,
  or promote.
- Monitoring records and sealed bundles now bind the exact canonical frozen
  monitoring-policy payload. Romano-Wolf inference exposes only the centered
  differential path; gate scopes and release-manifest link identity fail
  closed at direct-object boundaries.
- Historical-foundation source indexes are reconciled to the referenced
  Parquet bytes and actual row/session evidence. PASS releases cannot contain
  an empty immutable payload census.
- Synthetic hash-chain precommit-journal recovery now uses the same explicit
  authorized permit census as committed-history verification, while unlisted
  permits remain rejected. This does not recover an already-committed
  unanchored outcome tail. Prepared bundle candidates must satisfy the exact
  candidate schema before any signing bindings can be produced; identity
  values are no longer string-coerced.
- Direct XNYS calendar publication now requires an exact request-bound
  synthetic action permit and containing fixture root before mutation.
  Failed-Alpaca qualification publication resumes only exact recognized
  staged evidence after interruption and preserves unknown or tampered stages.
- Windows locks no longer share delete access, and shared relative paths reject
  Windows ADS/device/trailing-character aliases. Nasdaq symbol and
  corporate-action coverage timestamps now require exact canonical wire
  encodings.
- Effective-event corporate-action coverage now uses a signed schema-v5
  promotion bound to the retained process-date receipt, effective interval,
  asset census, provider contract, late-arrival policy, and current authority.
  The production path remains blocked while external authority is not
  configured.
- Alpaca network execution now binds caller-declared request time and the
  minimum end lag to a reachable 15-minute trusted-clock window before request
  authorization. Nasdaq `Nasdaq Traded=N` is retained and remains
  unknown/ineligible.
- Generic XNYS loading now reconstructs and reconciles all range and
  provenance claims. Legacy Alpaca duplicate state commits only after full row
  acceptance; feature releases require exactly one payload; release-selection
  epoch keys are no longer string-coerced.
- Legacy Alpaca daily evidence now requires exact New York-midnight timestamps,
  HFDL sidecar and row symbols must already be exact uppercase evidence, and
  public Alpaca provider annotations resolve their `Path` types at runtime.
- Corporate-action rows reject conflicting `ex_date`/`effective_date` evidence,
  daily-bar releases require the exact single payload, and zero-byte HTTP
  results are transport-bound before snapshot evidence rejects them as empty.
  The readiness contract now exposes only an explicitly non-authorizing
  mechanical status and no generic positive `ready` Boolean.
- XNYS calendar loading rechecks the installed package and derives the schema,
  policy, and environment closure before verification. HFDL date/time evidence
  must be native strings, research relative imports cannot escape the package,
  and corporate-action requests share the 15-minute trusted-time window.
- Completed-migration capsule identity and provenance fields must be exact
  strings before verification; the canonical publisher no longer normalizes
  malformed values into trusted migration evidence.
- Network-registry policy is immutable and revalidated against authenticated
  bytes. Plan-only qualification is filesystem-read-only; request-packet
  emission is an explicit mode. Corporate-action process-date acquisition and
  effective-event completeness remain separate evidence, isolation claims are
  bounded to static regression plus named public runtime tests, and migration
  recovery uses the public `.s` staging path.
- Historical-foundation loading now reconstructs code/environment closure and
  requires the exact singleton bridge-set payload. Bundle preparation rejects
  duplicate artifact declarations, and legacy Alpaca pagination tokens require
  exact nonempty string-or-null runtime values.
- Atomic file and Parquet publication now durably flushes parent-directory
  entry changes, including through a Windows directory handle. Synthetic
  direct-prediction permits bind the exact bundle, eligibility census, and
  ordered feature rows.
- Production prerequisites remain deliberate fail-closed limitations, and the
  stale non-active Nasdaq receipt remains immutable historical negative
  evidence rather than being cosmetically refreshed.
- Schema-v5 corporate-action publication and loading now bind every action to
  one exact governed source epoch, snapshot, asset scope, and effective-session
  interval. Schema-1 verified releases remain immutable/readable historical
  views but are explicitly not trust eligible.
- Ledger crash-recovery coverage now proves exact committed bytes, journal
  cleanup, and verification by a fresh public reader. The monitoring ledger's
  public synthetic-permit annotation resolves at runtime.
- Invalid ledger recovery journals are now preserved under an exact
  content-addressed rejected-evidence name, removed from the active journal
  slot durably, and rejected once; subsequent public reads verify the unchanged
  committed ledger. Prior-permit recovery has the same exact-byte, cleanup,
  reopen, and fresh-reader assertions as the primary path.
- Mechanical isolation now detects registered foreign projects as the first or
  an embedded exact path component without treating registry constants or
  prefix-like names as paths. Synthetic calendar construction normalizes both
  parser `TypeError` and `ValueError` to its stable contract error.
- Sensitive dynamic-execution imports now remain prohibited through local
  aliases. Corporate-action acquisition intervals cannot extend past the
  trusted request/latest landed-page UTC date, and malformed governed
  action/coverage enum or ISO-date evidence is normalized to `IntegrityError`.
- Unauthorized prior-permit recovery rejection now proves exact rejected-byte
  quarantine, active-journal cleanup, absence of committed history, and a
  successful subsequent fresh public read.
- Alpaca qualification now requires optional trade count `n` to be an exact
  non-Boolean nonnegative integer while preserving fractional `vw` support.
- The synthetic executor now freezes the signed next-open-to-fifth-close simple
  split-normalized price-return target under registration schema v2. Legacy
  schema-v1 registrations are rejected without migration or relabeling, so no
  previously frozen evidence is reinterpreted.
- Ledger recovery now reconstructs construction-bound record validators and
  append-only keys before accepting pending or already-committed journal tails;
  invalid or duplicate recovery evidence is quarantined without being blessed.
  Inner-fold audit identities are unique across the complete selection census
  at both builder-request and retained-artifact boundaries.
- Monitoring now keeps undersized evidence `PENDING` and abstaining after all
  invalid and explicit pause checks, before any warning classification.
  Release-backed outcome maturation rejects legacy/non-trust-eligible
  corporate-action ledgers before outcome construction; governed schema-v5
  coverage remains the accepted path.
- Failed or interrupted HFDL pair capsules are now atomically quarantined with
  a canonical byte-census receipt, retained indefinitely, and never reused.
  Publisher and public HFDL paths share one exact sidecar validator; undeclared
  compatibility fields are stripped and remain untrusted.
- Failed Windows lock acquisition retires only the authenticated open handle
  before close and never unlinks a post-close pathname. DSR mechanics require
  one immutable array binding over selected returns, the complete trial-Sharpe
  census, and exact sample identities.
- Merged identity state now becomes effective no earlier than the Alpaca
  observation boundary; later current state cannot be backdated to a Nasdaq
  file timestamp. Migration recovery preserves and rejects generic atomic
  write remnants before any plan-bound copy adoption.
- Environment validation compares all 22 direct and transitive distributions
  in the authenticated executable closure. The production authority registry
  rejects the explicit RFC test fixture ID or fingerprint, and shipped source
  is checked for private fixture material.
- Timestamp parsing rejects non-text inputs through `ContractError`, only
  split corporate actions may carry a split ratio, and every accepted provider
  action requires canonical asset attribution even for unfiltered requests.
- Outcome maturation now documents and tests one conservative action-and-bar
  evidence cutoff while retaining schema-v1 serialization, and unsupported
  corporate-action diagnostics name the actual comparison limitation.
- Filesystem assurance now explicitly covers cooperative project writers, not
  hostile same-principal namespace mutation. The guarded Alpaca provider path
  has an offline integration regression through real authorization, landing,
  detached attestation, verified reload, and tamper refusal with only transport
  substituted.
- Monitoring policy v1.1 now hash-binds the canonical
  `INVALID -> PAUSED -> PENDING -> WARNING -> OK` precedence; prior hashes are
  rejected without migration or relabeling. Release manifest construction
  rejects duplicate normalized caller paths before hashing while preserving
  the pre-change identity for unique input.
- Release publication now authenticates every staged source beneath the
  resolved staging root before pending creation and again immediately before
  use. A real Windows-junction regression proves external bytes cannot be
  copied through a linked ancestor.
- The latest local non-alpha run reported 584 passed and three platform-specific
  skips on 2026-07-27. This handoff is mutable context, not authenticated run
  evidence; rerun and retain external diagnostics before claiming current
  operating effectiveness. Routine v2 validation authenticates the immutable
  completed migration capsule instead of requiring the independently mutable
  legacy checkout to remain frozen forever.

## Source truth

- HFDL: legacy-discovery only; fixed/current universe and the documented
  PiTrading-to-IEX transition make it non-PIT confirmation evidence.
- Existing Alpaca: a 780-symbol, 1,878,977-row SIP capsule plus a separate
  30-symbol probe; qualification failed (coverage, gaps, unmapped identity,
  survivorship). Only exact native/checkpoint/snapshot/audit evidence may
  migrate; Parquet is regenerated.
- Prospective Alpaca: guarded and empty pending an authenticated, bounded feed
  qualification. SIP and IEX remain candidates; neither is assumed active.
- Nasdaq: one public as-received qualification snapshot is preserved, but it
  is not trust-eligible. Requalification requires an independently
  authenticated acquisition receipt before any asset join or identity release.
- Completed migration capsule: the immutable 4,911-file release is the
  post-migration source of truth. The allowlist, approval, and legacy baseline
  remain historical review evidence and are not replanned from current legacy
  files.

## Next gate

Stop at the controlled-rebuild boundary. Real-history hypothesis/WFA execution,
candidate sealing, destructive cutover, external push, and trading require new
authorization. `HISTORICAL_RESEARCH_READY` means only that the discovery
harness is mechanically runnable; PIT truth, exact legacy-trial census, alpha,
candidates, live use, options, and deployable shorts remain blocked. The next
source/authentication work must separately review and configure an external
public authority, capture one bounded fresh Nasdaq response, obtain its detached
signature outside the repository, and verify it offline before the source can
be requalified.
