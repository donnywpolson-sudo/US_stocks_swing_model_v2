# Audit Traceability

Each release-blocking finding must terminate in code, a synthetic test, and a
gate. `Milestone 1` means the core contract exists; later data/research tests
remain fail-closed until their milestone is authorized.

Current-state entries below are reconciled as of 2026-07-26. “Mechanics
implemented” means synthetic, non-authorizing enforcement exists; it does not
claim real-history evidence, PIT completeness, candidate eligibility,
prospective confirmation, production readiness, or a retained authenticated
test-run receipt. Test names specify required regression evidence, not current
operating-effectiveness proof.

| Finding | Required correction | Enforcement | Required regression evidence | State |
|---|---|---|---|---|
| A new repo does not restore a pristine holdout | All existing history remains discovery; prospective confirmation begins only after sealing | Constitution; registered trial ledger | Registry rejects unregistered or mutated trials | Milestone 1 |
| Real-data smoke is an uncounted trial | Only synthetic smoke is free; real-data evaluation requires registration | `trials.py` | Unregistered evaluation test | Milestone 1 |
| Hashes alone do not prove causality | Event/effective, available, retrieved, source epoch and release identity are mandatory | release/action/outcome schemas | Missing-time validation tests | Milestone 1 |
| Non-string JSON is silently coerced into release or bar identity fields | Require exact JSON scalar types before parsing; never call `str(...)` on manifest or daily-bar identity/time evidence | `releases.py`; `outcomes.py` | numeric/Boolean poison tests against self-consistent accepted containers | Milestone 1 |
| HFDL source break and hindsight limits | Isolate two HFDL epochs as legacy discovery; never pool with Alpaca | source config and migration roles | Configuration invariant tests | Milestone 1 |
| Existing Alpaca capsule failed quality | Preserve native/checkpoint/snapshot/audit evidence only; regenerate Parquet and retain failure state | source and migration configs | Scope/role/exclusion invariant tests | Milestone 1 |
| Free source can silently change entitlement/feed/adjustment/mapping | Bounded SIP-versus-IEX qualification; activate only the evidenced feed; pin 1Day/raw/ascending/minimum lag and omit `asof` unless an ISO date is deliberate | `providers/alpaca.py`; qualification CLI | Request-policy/feed/byte-bound tests | Milestone 2A |
| Alpaca alone cannot safely classify all security types | Add as-received Nasdaq identity; unknown/nonstandard types abstain | `providers/nasdaq.py`; inference | ETF/stock/unknown fixtures | Milestone 1 |
| Nasdaq marks a nonstandard instrument as an ETF | Apply warrant/unit/right/preferred/note and other nonstandard-name exclusions before ETF eligibility | `providers/nasdaq.py` | ETF-flagged warrant plus ordinary ETF/stock controls | Milestone 1 |
| Corporate actions are revisable | Append bitemporal revisions; separate as-received and final views | `corporate_actions.py` | Visibility/revision tests | Milestone 1 |
| Row shifts cross missing sessions and delistings | Use a pinned exchange-session calendar and explicit terminal statuses | `calendar.py`; `outcomes.py` | holiday/missing/halt/delist/action tests | Milestone 1 |
| Adjusted prices can embed hindsight | Use raw bars; compute split-normalized price outcome from versioned actions | outcome engine | split fixture and unresolved action fixture | Milestone 1 |
| Feature/label/prediction coupling leaks future truth | Separate schemas and forbidden-field checks | `schemas.py` | poison-field tests | Milestone 1 |
| Evaluator-guided repair overfits | Registration hash freezes semantics; changes create new trial | `trials.py` | mutation/re-evaluation tests | Milestone 1 |
| Aggregate results hide subgroup failure | Bind stock/ETF and long/short sleeve gates independently | `gates.py` | failed/underpowered sleeve tests | Milestone 1 |
| PBO values exactly at a registered ceiling evade the stricter adjacent state | Treat equality at the conservative maximum as inconclusive and equality at the failure threshold as failure | `gates.py` | exact-boundary state tests | Milestone 1 |
| Fold model is not a deployable model | Seal full bundle identity and verify every artifact hash | `bundle.py` | mutation/reload-parity tests | Milestone 1 |
| Current bundle schema advertises an unreachable ready state | Make schema v3 explicitly blocked and always non-trust-eligible; reject `VERIFIED_READY` until a separately reviewed schema verifies real readiness and external anchors | `bundle.py` | blocked-state load plus attempted ready-state rejection | Milestone 1 |
| Sealed bundle metadata silently coerces identity, time, and hash fields | Require exact JSON strings and exact nested census/evidence container types before constructing metadata | `bundle.py` | hash-consistent scalar and nested-type poison tests | Milestone 1 |
| Bundle path traversal test does not exercise bundle preparation | Validate every artifact path at the public preparation boundary before permit work or any artifact hash read | `bundle.py` | `prepare_bundle_candidate` parent-path test with a hash-read tripwire | Milestone 1 |
| Daily scoring retrains | Fit-free artifact loader and inference engine expose no training API | `inference.py` | `.fit()` spy/absence tests | Milestone 1 |
| Option proxy fields overstate the claim | Prediction schema permits underlying-only fields | `schemas.py`; inference | forbidden-option-field tests | Milestone 1 |
| Prediction deserialization silently coerces identity and provenance values | Require exact nonempty text at both serialized and direct-object validation boundaries before recomputing prediction identity | `schemas.py` | hash-consistent serialized and direct-object identity poison tests | Milestone 2A |
| Prediction rows can be backdated, rewritten, truncated, or include outcomes | Actual-time bounded inference; canonical IDs over full input/output; hash chain plus separate content-addressed head anchor; separate outcome ledger | `inference.py`; `ledger.py` | backdate/post-entry/ID/rewrite/truncation/outcome-field tests | Milestone 2A |
| Synthetic ledger or recovery evidence is replayed under another fixed-time permit | Require every synthetic record to belong to the verifier's explicit permit census; clock rebinding carries only previously verified permits plus the new repository clock, while pending recovery and new anchors require the exact current permit | `ledger.py`; `trials.py` | direct cross-permit rejection, governed clock-rebind history, and wrong-permit recovery rejection | Milestone 2A |
| Extra accepted bar history prevents a valid five-session outcome from maturing | Filter the verified release population by exact asset and calendar-derived entry-through-exit sessions before calling the strict outcome builder | `ledger.py`; `outcomes.py` | long-history plus foreign-asset interval selection test | Milestone 2A |
| Terminal outcome states can observe a bar before that bar is available | Require the action view to be at or after every relevant present bar's `available_at` before resolving missing, halted, delisted, or matured state | `outcomes.py` | future-availability tests for unresolved and terminal states | Milestone 2A |
| A timestamp-shaped value enters a date-only feature, prediction, or outcome field | Require exact `date`, not the superclass-compatible `datetime`, and reject non-string serialized dates before parsing | `schemas.py` | direct datetime and serialized numeric-date poison tests across all three row contracts | Milestone 2A |
| Direct feature or outcome construction carries a string where serialization expects an enum | Require exact `SecurityType` and `OutcomeStatus` instances during validation before any `.value` serialization access | `schemas.py` | direct string-enum poison tests for both row contracts | Milestone 2A |
| Partial publish or concurrent writers corrupt releases | One-writer exclusive locks, staging verification, atomic rename, owner-disposition orphan quarantine | `releases.py`; `locking.py`; `docs/PENDING_RELEASE_LIFECYCLE.md` | crash/idempotence/lock tests | Milestone 1 |
| Legacy file discovery or stale approval imports clutter | Deny-unlisted migration with approved roots; no links/overwrites; execution approval binds exact config/inventory/plan/code/count/bytes | `migration.py`; CLI | alternate-root/hardlink/approval-drift/exact-tree tests | Milestone 2A |
| A final migration source path is a symlink even though source roots are approved | Reject links explicitly at the source-root, discovered-source, and execution-reverification boundaries in addition to containment checks | `migration.py` | direct final-path link rejection and existing containment/hardlink tests | Milestone 2A |
| Provider or copy command runs accidentally | Filesystem-read-only plan default; separate explicit request-emission mode; execution flag, environment token, and signed authority | both CLIs | no-write plan, explicit/deprecated emission, and execution guard tests | Milestone 1 |
| One-shot response evidence is consumed before a fallible snapshot landing completes | Validate without spending, perform all capability/clock checks, and mark the response consumed only after the serialized landing commit returns successfully under the response lock | `providers/network_execution.py`; `providers/snapshots.py` | simulated landing failure followed by one successful retry and replay rejection | Milestone 1 |
| Environment lock schema version uses Python equality and admits Boolean one | Require an exact JSON integer schema version before any runtime or dependency comparison | `environment.py` | Boolean schema poison plus real-lock validation | Milestone 1 |
| Interrupted copy exposes partial evidence | Resume from verified checkpoints and atomically promote only the complete content-addressed plan | `migration.py` | public `execute_copy_plan` crash/resume at the approval-bound `.s` stage, tamper rejection, and family-receipt tests | Milestone 2A |
| Source mutates during a long copy | Recheck each source before/after copy and the full plan before promotion | `migration.py` | mutation and hash tests | Milestone 2A |
| Legacy Parquet/source transitions become active truth | Validate HFDL sidecars/bytes and tag both epochs as non-PIT legacy discovery | `canonical/hfdl.py` | feed-break and clean-room tests | Milestone 2A |
| Independent HFDL validation silently coerces sidecar identity or denominator evidence | Enforce exact public-validator JSON types, positive integer row counts, SHA-256 syntax, canonical UTC `Z` creation time, and string-only limitations before comparison or normalization | `canonical/hfdl.py` | Boolean/numeric/string denominator, identity, time, and limitation poison tests | Milestone 2A |
| Derived Alpaca Parquet hides raw/provenance defects | Reparse explicit raw gzip page manifests; retain failed quality/non-active state | `canonical/alpaca.py` | pagination/duplicate/schema/determinism tests | Milestone 2A |
| Legacy Alpaca timestamps are normalized before their claimed UTC encoding is checked | Require the original native JSON value to be an exact canonical UTC `Z` string before deriving a session | `canonical/alpaca.py` | nonzero-offset, alternate-zero-offset, and non-string timestamp poison tests | Milestone 2A |
| Legacy Alpaca symbol evidence is normalized before its wire form is validated | Require exact nonempty uppercase symbol keys and reject lowercase or padded native evidence before identity lookup or duplicate tracking | `canonical/alpaca.py` | lowercase, padded, and mixed-case symbol poison tests | Milestone 2A |
| Legacy Alpaca price fields silently accept Boolean or string coercions | Require exact non-Boolean JSON numerics for OHLC and optional VWAP before conversion or domain validation | `canonical/alpaca.py` | Boolean, string, null, and valid numeric price tests | Milestone 2A |
| Identity files are parsed before evidence is preserved | Atomically land bytes, headers and receipt before strict comprehensive-file parse | `providers/snapshots.py`; `providers/nasdaq.py` | tamper/header/trailer/identity tests | Milestone 2A |
| Nasdaq local creation time falls in a DST gap or repeated fold | Validate by UTC round-trip and reject both nonexistent and ambiguous wall times because the provider supplies no fold discriminator | `providers/nasdaq.py` | spring-gap, fall-fold, ordinary-time, and future-time tests | Milestone 2A |
| Snapshot URL evidence is silently string-coerced or accepts unsafe URL forms | Require an exact string containing a credential-free HTTPS URL without fragments before hashing, landing, or loading | `providers/snapshots.py` | numeric, non-HTTPS, credential, fragment, whitespace, and control-character tests | Milestone 2A |
| A content-addressed snapshot receipt can encode impossible response status or payload size | Recheck HTTP status and positive bounded raw-byte evidence at every load before identity acceptance | `providers/snapshots.py` | hash-consistent invalid-status and empty-payload reload tests | Milestone 2A |
| Authenticated network policy can be changed in memory | Store operative origin/status maps as read-only proxies and re-derive/compare them with authenticated registry bytes on every validation | `providers/snapshots.py` | direct-mutation rejection, forced-replacement detection, and compatible capability issuance | Milestone 2A |
| Duplicate query keys collapse into a lossy request mapping | Reject duplicate keys in initial URLs and pagination requests; preserve and compare the exact raw base query while allowing exactly one verified page token | `providers/network_execution.py` | initial-plan and paginated duplicate-key tests through local execution entry points | Milestone 2A |
| Snapshot trust status remains cached after external authority drift | Revalidate the signed acquisition attestation and current pinned authority on every `trust_eligible` decision; fail closed to `False` on any validation error | `providers/snapshots.py` | active-to-revoked authority drift test plus verified-byte rejection | Milestone 2A |
| Missing/reused symbols remain eligible by carry-forward | Complete bitemporal membership snapshots, UTC-normalized file-time causality, explicit absence tombstones with null Nasdaq membership evidence, and no reuse inheritance | `identity.py`; `providers/nasdaq.py` | disappearance/reuse/change/late-revision/future-file-time/UTC tests | Milestone 2A |
| Nasdaq-only identity later splits from its Alpaca-resolved asset identity | Use a stable reserved unresolved-symbol ID, require it to remain ineligible, and suppress its superseded tombstone once the same symbol has a current resolved member | `identity.py` | unresolved-to-resolved historical/current visibility test | Milestone 2A |
| Provider process dates are mistaken for causal corporate-action completeness | Separate `PROVIDER_PROCESS_DATE_ACQUISITION_ONLY` from `EFFECTIVE_EVENT_COMPLETENESS`; require an exact signed schema-v5 promotion bound to the full provider receipt, effective interval, asset census, provider contract, late-arrival policy, release epoch, and current authority; outcomes consume only the authorized result | `providers/corporate_actions.py`; `corporate_actions.py`; `outcomes.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | missing/mismatched authority, contract-policy drift, cross-boundary effective-date, legacy-schema, distinct-container-ID, and incomplete-window tests | Milestone 2A mechanics implemented; production remains blocked while external authority is not configured |
| Verified corporate-action releases silently coerce identity or provenance values | Require exact JSON strings for action identity, enum, dates, timestamps, and source evidence before construction | `corporate_actions.py` | hash-consistent non-string action release poison tests | Milestone 2A |
| Direct corporate-action coverage accepts truthy non-string census or epoch values | Require exact text for coverage semantics, scope, every asset ID, and the source epoch at the direct-object boundary | `corporate_actions.py` | direct numeric asset-ID and source-epoch poison tests | Milestone 2A |
| Payable date is treated as an action's economic effective session | Use only explicit ex-date or effective-date evidence; payable-only rows remain unresolved rather than affecting an outcome interval | `providers/corporate_actions.py` | payable-only action normalization test | Milestone 2A |
| A multi-party corporate action is collapsed or rejected because only one symbol can be retained | Preserve every recognized participant in exact `involved_symbols` evidence, retain the provider primary symbol separately, bind the immutable raw row, and require request-census intersection | `providers/corporate_actions.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | two-party merger preservation, exact-array poison, and unrequested-census tests | Milestone 2A |
| Corporate-action participant symbols are silently canonicalized | Require every recognized raw participant field to already be exact uppercase canonical text before preserving its identity | `providers/corporate_actions.py` | lowercase, padded, and mixed-case participant poison tests | Milestone 2A |
| Mechanical-readiness publication can mutate generated artifacts without independent action authority | Require an exact external `AUTHORIZE_MECHANICAL_READINESS_PUBLICATION` receipt bound to the assessment, foundation, roots, timestamp, two outputs, and production UTC clock before creating output paths | `mechanical_readiness.py`; `cli/assess_mechanical_readiness.py`; `docs/EXTERNAL_AUTHORIZATION.md` | missing-authorization pre-mutation and exact-binding tests | Milestone 1 |
| Signed authorization bindings normalize non-string wire values | Require exact string keys and values before receipt construction and again at direct-object validation | `governance.py` | Boolean binding poison test | Milestone 2A |
| A rejected Alpaca bar changes later timestamp-order validation | Commit per-symbol ordering state only after required and optional bar predicates all succeed | `providers/alpaca.py` | invalid optional field followed by a valid identical timestamp | Milestone 2A |
| A non-midnight Alpaca row poisons later timestamp ordering | Commit timestamp ordering state only after the accepted daily-bar timestamp also passes the New York-midnight contract | `providers/alpaca.py` | later non-midnight row followed by an earlier valid daily timestamp | Milestone 2A |
| Alpaca qualification accepts volume evidence that canonical OHLCV cannot represent | Require provider daily-bar volume to be an exact non-Boolean nonnegative JSON integer before qualification | `providers/alpaca.py` | fractional, integer-valued float, Boolean, string, negative, and valid integer volume tests | Milestone 2A |
| Caller-controlled Alpaca request time weakens the minimum end lag | Before authorization or transport, require `requested_at` to be no later than and at most 15 minutes older than the trusted request clock, then recheck the end lag against that trusted instant | `providers/alpaca.py` | exact-window boundary, future/stale request, trusted end-lag, and pre-authorization rejection tests | Milestone 2A |
| One-fold temporal concentration divides by an empty retained census | Skip undefined leave-one-out arithmetic and return the registered insufficient-fold inconclusive state | `research/robustness.py` | single-fold controlled-inconclusive test | Milestone 3 mechanics implemented; real-history execution blocked |
| Local network sessions enter the weak registry outside its synchronization lock | Insert, look up, mutate, and consume issued-session/response state only under the shared registry lock | `providers/network_execution.py` | single-use, replay, and concurrent local execution controls | Milestone 1 |
| Alternate-root or pending release silently wins | Select exactly one accepted manifest path with dataset/epoch/role allowlists | `source_selection.py` | pending/wrong-role tests | Milestone 2A |
| Reserved release-name checks reject unrelated ancestor names | Match only the exact relative `.staging`, `.quarantine`, and `.pending-*` forms beneath the accepted root; never substring-scan the root or ancestors | `source_selection.py` | accepted root containing `pending-review` plus exact reserved-form rejection | Milestone 2A |
| Large correlated row count overstates evidence | Date/session effective sample and block/HAC methods | `research/hac.py`; `research/bootstrap.py`; registered synthetic executor | Synthetic dependence and effective-sample tests | Milestone 3 mechanics implemented; real-history execution blocked |
| Tuning leaks across WFA folds | Nested chronological folds, interval purge, fold-local transforms, and capability-restricted outer-label access | `research/splits.py`; `research/builder.py`; `research/executor.py` | Synthetic leakage, poison, and structural isolation tests | Milestone 3 mechanics implemented; real-history execution blocked |
| A separately consistent synthetic evaluation can name a different audit census | Bind every fold evaluation’s ordered audit sample IDs to the exact frozen prediction artifact census in aggregate execution validation | `research/executor.py` | recomputed evaluation-ID and execution-ID replacement test | Milestone 3 mechanics implemented; real-history execution blocked |
| Finite AST scans are mistaken for a runtime sandbox | Preserve the `STATIC_SOURCE_AST_ONLY_NOT_RUNTIME_PROOF` boundary and separately test the public executor’s runtime data capability with filesystem/network entry points disabled | `mechanical_readiness.py`; `research/executor.py`; `docs/ISOLATION_ASSURANCE_BOUNDARY.md` | dynamic request inspection, no-I/O execution, label poison, and fit-free role tests | Static regression plus selected public-path runtime evidence only; no hostile-code sandbox claim |
| An allowed numerical import exposes unrecognized file-I/O capabilities | Resolve NumPy import aliases and reject its file-loading, persistence, mapping, and text/binary I/O entry points while permitting pure numerical calls | `tests/test_research_controls_sleeves_boundary.py` | direct, aliased, submodule I/O fixtures plus a pure-NumPy control | Static regression control |
| Approved foreign-project literals can be relocated within the same file | Bind each exception to its exact file, assignment target, AST role, normalized literal, and count | `tests/test_project_isolation.py` | approved-placement census plus same-file relocation rejection | Static regression control |
| Prospective evidence is inspected or rewritten early | Separate append-only prediction/outcome truth ledgers, fixed end rule, fit-free evaluation, and mandatory separate local head anchors | `ledger.py`; `outcomes.py`; `research/evaluator.py`; `docs/OUTCOME_LEDGER_ANCHOR_POLICY.md` | clock, anchor replacement/truncation, recovery, revision, census, and fit-free tests | Milestone 5 mechanics implemented; prospective execution blocked |
| Monitoring silently changes the model | Monitoring may only alert, pause, or abstain and remains bundle/policy/reference bound | `monitoring.py`; `config/prospective_monitoring_policy.json` | mutation-attempt, bounds, recovery, and bundle-binding tests | Milestone 6 mechanics implemented; production monitoring blocked |
| A direct HFDL publisher bypasses the plan-only foundation boundary | Require either a synthetic action permit bound to the exact contract and containing fixture root or the exact checked-in one-shot non-active successor-refresh authorization; bind production roots, capsule, timestamp, clean commit distance, and idempotent same-build recovery before mutation | `canonical/hfdl_legacy_publisher.py`; `foundation_orchestrator.py`; `config/foundation_refresh_authorization.json` | pre-mutation missing/wrong-authorization rejection, exact authorization hash, root containment, restart, and orchestrator compatibility tests | Milestone 2A mechanics plus one bounded non-active successor refresh |
| Failed Alpaca qualification evidence is published without action authority | Require a synthetic publication permit bound to the exact table, evidence, paths, timestamp, and hashes plus an explicit containing fixture root before staging | `canonical/alpaca.py` | missing-permit pre-mutation and deterministic publication tests | Milestone 2A synthetic mechanics only |
| Monitoring records name a policy hash unrelated to the evaluated policy | Derive one canonical frozen policy payload/hash, require it in ledger and bundle construction, and compare the supplied evaluated policy before assessment | `monitoring_policy.py`; `monitoring.py`; `bundle.py` | arbitrary-hash rejection, record replay, bundle poison, and recovery tests | Milestone 6 mechanics implemented; production monitoring blocked |
| A public Romano-Wolf primitive accepts uncentered bootstrap statistics | Keep the statistic-level primitive private, require an explicit true null-centered assertion, and publicly expose only the differential wrapper that constructs the centered null | `research/multiple_testing.py`; `research/__init__.py` | uncentered rejection, public-export absence, oracle, and centered-wrapper tests | Milestone 3 mechanics implemented; real-history execution blocked |
| Standalone gate receipts accept unknown evaluation scopes | Require exact `OUTER_SCREEN` or `FINAL_HOLDOUT` scope during direct and deserialized receipt validation | `gates.py` | empty and unknown scope poison tests through `GateReceipt.from_dict` | Milestone 3 mechanics implemented; real-history execution blocked |
| Authoritative release manifests lack an explicit independent-file check | Require the manifest itself to have exactly one hardlink before parsing, in addition to the exact-tree check | `releases.py` | external manifest-hardlink rejection test | Milestone 1 |
| HFDL source-index declarations drift from the referenced Parquet evidence | Reconcile every declared path, plain-file identity, size, hash, row count, symbol, and session range against the actual object before bridge derivation or reload | `historical_foundation.py` | valid index plus hash, size, row-count, and session-bound poison tests | Milestone 2A |
| A manifest-only tree represents a successful trust-bearing release | Require every PASS release role to carry a nonempty immutable payload census during construction and deserialization | `releases.py` | empty active, prospective, derived, feature, and outcome release construction/reload tests | Milestone 1 |
| Synthetic ledger rebinding strands a valid precommit journal from an authorized prior permit | Apply the same explicit authorized permit census to committed history and precommit-journal recovery; continue rejecting every unlisted permit; do not treat this as recovery of an already-committed unanchored outcome tail | `ledger.py`; `docs/OUTCOME_LEDGER_ANCHOR_POLICY.md` | authorized prior-permit journal recovery and foreign-permit rejection tests | Synthetic precommit mechanics only; outcome-anchor recovery remains unsupported |
| Prepared bundle signing stringifies malformed candidate fields | Validate the exact candidate-only schema and nested evidence before producing bindings, then use exact string values without coercion | `bundle.py` | canonical hash-valid non-string signing-field poison tests | Milestone 4 mechanics implemented; production sealing blocked |
| Direct calendar publication bypasses the repository action boundary | Require an exact request-bound synthetic publication permit and explicit containing fixture root before any staging or release mutation | `exchange_calendar.py` | missing/mismatched permit pre-mutation tests plus authorized caller compatibility | Milestone 2A synthetic mechanics only |
| Generic calendar loading trusts unreconciled provenance claims | Reconstruct the pinned requested-range schedule and reconcile package, creation time, first/last session, early-close count, row metadata, and manifest bounds against verified content | `exchange_calendar.py` | hash-consistent range, package, boundary, count, and creation-time poison tests | Milestone 2A |
| Interrupted failed-Alpaca publication strands a nonempty stage | Publish each staged file atomically, resume only exact recognized bars-only or complete states, and preserve unknown or tampered content fail-closed | `canonical/alpaca.py`; `canonical/parquet.py` | interruptions before and after summary commit, exact retry, tamper rejection, and unknown-content preservation | Milestone 2A synthetic mechanics only |
| Rejected legacy Alpaca rows mutate duplicate-detection state | Complete every row predicate before committing the symbol/session key or derived output state | `canonical/alpaca.py` | invalid row leaves the duplicate census empty and a valid identical key remains acceptable | Milestone 2A |
| Legacy daily Alpaca timestamps accept intraday instants | Require each canonical UTC timestamp to map to exact New York midnight before deriving its session or committing duplicate state | `canonical/alpaca.py` | intraday timestamp rejection plus valid midnight compatibility | Milestone 2A |
| HFDL identity comparison normalizes symbol case | Require the sidecar symbol and every native ticker to already equal exact uppercase canonical evidence; do not uppercase before comparison | `canonical/hfdl.py` | lowercase sidecar and hash-valid lowercase row poison tests | Milestone 2A |
| Feature releases permit extra manifest-declared payloads | Require the authoritative feature payload census to equal exactly `{features.json}` | `feature_release.py` | valid features payload plus independently hashed extra-file rejection | Milestone 2A |
| Release selection stringifies non-string epoch keys | Require exact nonempty string epoch keys and exact valid string role values without coercion | `source_selection.py` | string-like object key rejection plus valid compatibility tests | Milestone 2A |
| Public Alpaca annotations reference an unbound `Path` name | Import `pathlib.Path` in the provider module and resolve the public function annotations at runtime | `providers/alpaca.py` | `typing.get_type_hints` resolution test | Milestone 2A |
| Windows lock handles share delete access | Open the owned lock without `FILE_SHARE_DELETE`, preventing pathname deletion or replacement while retaining exact-handle retirement | `locking.py` | live Windows delete/replace denial, overlap rejection, and exact-handle release tests | Milestone 1 |
| Shared relative paths admit Windows-special aliases | Reject ADS separators, reserved device names, forbidden/control characters, empty segments, and trailing dots/spaces before any filesystem join | `common.py` | ADS, device, trailing-character, duplicate-separator, traversal, and compatible nested-path tests | Milestone 1 |
| Nasdaq symbols are normalized before wire validation | Require the provider symbol to already be nonempty, trimmed, uppercase canonical wire text | `providers/nasdaq.py` | lowercase, padded, and mixed-case symbol poison tests | Milestone 2A |
| Nasdaq `Nasdaq Traded=N` is discarded before eligibility | Retain the exact traded flag, classify `N` as unknown, and require `Y` in the downstream eligibility predicate | `providers/nasdaq.py`; `identity.py` | nontraded common-stock classification and eligibility test | Milestone 2A |
| Effective-event coverage accepts noncanonical offset timestamps | Require canonical UTC `Z` receipt time before content-ID reconstruction or authorization validation | `corporate_actions.py` | hash-consistent nonzero-offset schema-v5 coverage test | Milestone 2A |
| Corporate-action rows silently prefer `ex_date` over a conflicting `effective_date` | Parse both canonical dates and reject the row unless every supplied effective-session field agrees | `providers/corporate_actions.py` | matching dual-date control plus conflicting-date poison response | Milestone 2A |
| Mechanical readiness exposes a generic positive `ready` Boolean beside blocked production states | Remove the generic Boolean, expose only an explicit non-authorizing legacy-discovery mechanical status, and enforce the exact readiness field census | `config/research_readiness_contract.json`; `mechanical_readiness.py` | status contract assertions plus injected generic-ready rejection | Milestone 3 mechanics only; candidate and production remain blocked |
| Daily-bar loading accepts authenticated but unauthorized extra payload files | Require the manifest payload census to equal exactly `{daily_bars.json}` before parsing | `outcomes.py` | valid daily bars plus independently hashed extra-file rejection | Milestone 2A |
| Empty HTTP bodies are rejected as request-binding failures | Bind the exact zero-byte transport result, then reject it as empty snapshot/source evidence before provider parsing or local verification | `providers/network_execution.py`; `providers/snapshots.py` | zero-byte response binding plus snapshot-evidence rejection | Provider execution remains explicit and disabled by default |
| Calendar loading recomputes schedules under an unpinned installed package | Recheck the installed `exchange-calendars` version at every load before schedule reconstruction | `exchange_calendar.py` | publish under the pinned version, simulate runtime drift, and reject on load | Milestone 2A |
| Relative imports bypass the research capability AST policy | Resolve package-relative imports by file depth, allow only imports that remain inside `research`, and reject every parent escape | `tests/test_research_controls_sleeves_boundary.py`; `docs/ISOLATION_ASSURANCE_BOUNDARY.md` | top-level and nested escape poison tests plus valid local/parent-package controls | Static regression control |
| HFDL date/time evidence is string-coerced | Require exact native string values before parsing `YYYYMMDD` or comparing `000000`; perform no coercion | `canonical/hfdl.py` | public-validator date/time non-string poison tests with otherwise exact schema evidence | Milestone 2A |
| Calendar verification receipts include unverified closure declarations | Derive and compare the schema fingerprint plus current pinned calendar-policy and environment hashes before receipt construction or publication | `exchange_calendar.py` | independently authenticated schema/config/environment manifest poison tests | Milestone 2A |
| Corporate-action acquisition has no trusted request-freshness policy | Apply the shared 15-minute Alpaca trusted-clock window before issuing the first authorized request attempt | `providers/corporate_actions.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | exact-window control plus future/stale direct and guarded-entrypoint rejection | Provider execution remains separately authorized and disabled |
| Completed-migration identity/provenance scalars are string-coerced | Require exact strings for migration ID, role, source, destination, digest, and payload object before validation or object construction; remove all constructor coercion | `canonical/hfdl_legacy_publisher.py` | self-consistent hash-updated boolean, number, null, array, and object poison capsules for migration ID/source/destination | Milestone 2A |
| Authenticated network-registry policy can be mutated after load | Store normalized origin/status policy in immutable mapping proxies and reconstruct the operative policy from authenticated registry bytes during every validation | `providers/snapshots.py` | direct mutation rejection, object-replacement divergence detection, and valid capability compatibility | Provider execution remains separately authorized and disabled |
| Process-date acquisition coverage is treated as effective-event completeness | Preserve provider process-date acquisition receipts as non-operative evidence and require separately signed, release-bound effective-event completeness for the exact outcome interval | `providers/corporate_actions.py`; `corporate_actions.py`; `outcomes.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | schema-v5 authorization, interval/asset/release poison, and missing-completeness outcome tests | Production authority remains `NOT_CONFIGURED` |
| Static AST scans are described as runtime isolation proof | Bound AST checks to finite source-regression assurance and exercise the reviewed public executor with label capability inspection plus filesystem/network entry points disabled | `tests/test_project_isolation.py`; `tests/test_research_controls_sleeves_boundary.py`; `tests/test_research_executor.py`; `docs/ISOLATION_ASSURANCE_BOUNDARY.md` | recursive AST poison cases and public-path runtime capability tests | Static regression plus named runtime path only; no OS sandbox claim |
| Migration recovery is tested only through a private verifier at a stale staging path | Interrupt and resume through public `execute_copy_plan` at the approval-bound `.s/<plan>.<approval>` stage; adopt only an exact reviewed copy and reject tampering | `migration.py`; `tests/test_migration_and_config.py` | public pre-checkpoint crash/resume, exact adoption, tamper rejection, and current-stage assertion | Synthetic controlled-copy mechanics only |
| Historical-foundation manifests are loaded without reconstructing code/environment closure | Recompute current implementation and environment hashes and compare them to both the bridge-set manifest and all six physical release manifests | `historical_foundation.py` | set-level and physical-release code/environment tamper tests | Milestone 2A |
| The historical-foundation bridge-set loader accepts a non-singleton declared payload census | Require the set manifest payload paths to equal exactly `{bridge_set.json}` before parsing | `historical_foundation.py` | extra declared set-payload rejection plus valid singleton compatibility | Milestone 2A |
| Bundle preparation silently deduplicates artifact declarations | Normalize each declared relative path, reject duplicate or normalization-equivalent declarations, then sort only the already unique census | `bundle.py` | duplicate artifact declaration rejection plus existing valid sealing path | Milestone 4 mechanics implemented; production sealing blocked |
| Legacy Alpaca pagination tokens rely on annotations rather than runtime type checks | Require manifest input/expected tokens and raw response next tokens to be exact nonempty strings or null before chain comparison or propagation | `canonical/alpaca.py` | Boolean, numeric, empty, array, and object token poison tests plus valid pagination compatibility | Milestone 2A |
| Atomic rename/link publication returns before its directory entry is durably committed | Fsync the parent directory after replacement and after both hard-link publication and temporary-name retirement; use a write-capable directory handle with backup semantics on Windows | `common.py`; `canonical/parquet.py` | parent-sync call census, injected sync failure propagation, and canonical-Parquet delegation | Milestone 1 |
| A synthetic direct-prediction permit authorizes any inference subject sharing its scope | Bind the permit fixture identity to the exact bundle, eligibility census, and ordered feature-row hashes before prediction mechanics execute | `inference.py` | wrong-bundle, wrong-census, reordered-row, and valid compatibility tests | Synthetic non-publishable mechanics only |
| Open production prerequisites are mistaken for repository defects | Keep PIT, exact-trial-census, external-anchor, production-census, holdout, and readiness blockers binding; classify them as limitations unless a bypass or false readiness claim exists | `config/research_readiness_contract.json`; `docs/ASSESSMENT_SCOPE_AND_BLOCKER_DISPOSITIONS.md` | explicit blocker assertions and attempted ready-state rejection | Deliberately blocked pending separately authorized external/real evidence |
| A preserved historical Nasdaq receipt is treated as a current credential to refresh | Retain the original non-active receipt and event-bound hashes; require a new independently authenticated acquisition receipt for any future qualification | `config/nasdaq_qualification_receipt.json`; `config/sources.json`; `docs/ASSESSMENT_SCOPE_AND_BLOCKER_DISPOSITIONS.md` | self-hash, stale-closure, non-active, and non-trust-eligible source-status assertions | Preserved negative evidence; not current authority |
| Schema-v5 action rows can borrow unrelated coverage from the same release | Require every action to match one governed row by source epoch, source snapshot, asset scope, and effective-session interval; repeat the check when loading verified bytes | `corporate_actions.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | wrong-asset and out-of-interval builder rejection plus authenticated-payload loader rejection | Production completeness remains blocked while authority is `NOT_CONFIGURED` |
| Legacy schema-1 action releases are labeled trust eligible despite lacking governed completeness | Keep the verified payload view immutable and readable, but mark trust eligibility true only for schema 5 after governed coverage verification | `corporate_actions.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | schema-1 non-trust assertion, immutable append rejection, and schema-5 trust control | Legacy historical rows only |
| Precommit recovery test checks logical IDs without proving disk cleanup or fresh-reader verification | Assert exact committed ledger bytes, journal removal, and successful verification through a newly constructed public ledger reader | `tests/test_source_selection_and_recovery.py` | authentic single-record crash-recovery regression | Synthetic recovery mechanics only |
| Monitoring constructor exposes an unresolved public annotation | Import `SyntheticOnlyPermit` at module runtime and resolve the full constructor annotation map | `monitoring.py`; `tests/test_research_robustness_monitoring.py` | `typing.get_type_hints` public-constructor regression | Milestone 5 mechanics |
| Invalid recovery journals can repeatedly block the public reader with no disposition contract | Verify committed history first, content-address and durably quarantine exact rejected bytes, raise the rejecting read, and permit later public verification of unchanged committed history | `ledger.py`; `docs/LEDGER_RECOVERY_JOURNAL_LIFECYCLE.md` | adversarial envelope, truncated JSON, malformed batch, and non-object journal lifecycle tests | Local recovery mechanics only |
| Authorized-prior-permit recovery has weaker persistence postconditions | Require exact committed bytes, active-journal removal, reopen, permit-census reauthorization, and fresh public verification in that branch | `tests/test_source_selection_and_recovery.py` | prior-permit crash-recovery regression with a fresh reader | Synthetic recovery mechanics only |
| Mechanical isolation misses a foreign project at the beginning of a path literal | Compare exact normalized path components, including the first component, while excluding exact registry/guard names and unrelated prefix-like components | `mechanical_readiness.py`; `docs/ISOLATION_ASSURANCE_BOUNDARY.md` | first-component, embedded-component, separator, exact-name, and prefix-like regression cases | Static regression control only |
| Synthetic calendar parsing leaks `TypeError` for non-string sessions | Translate both parser `TypeError` and `ValueError` to the existing stable `ContractError` at the narrow parse boundary | `calendar.py`; `tests/test_calendar_actions_outcomes.py` | null, integer, malformed-string, and valid-session behavior | Synthetic calendar mechanics only |
| Imported aliases bypass sensitive dynamic-execution detection | Precompute bindings for prohibited `ImportFrom` symbols, reject both their imports and later alias references, and preserve unrelated ordinary aliases | `mechanical_readiness.py`; `docs/ISOLATION_ASSURANCE_BOUNDARY.md` | aliased `eval`, `exec`, `__import__`, and `import_module` plus harmless alias control | Static regression control only |
| Completed provider coverage can claim future process dates | Bound request end to the trusted UTC acquisition date and completed coverage end to the latest landed-page UTC date, with equality allowed | `providers/corporate_actions.py`; `docs/CORPORATE_ACTION_COVERAGE_SEMANTICS.md` | exact-boundary, future-request, and future-completed-coverage tests | Process-date acquisition evidence only |
| Corporate-action release loading leaks raw enum/date parser errors | Translate invalid action enums and action/coverage ISO dates to the loader's `IntegrityError` boundary while preserving causes | `corporate_actions.py`; `tests/test_calendar_actions_outcomes.py` | unknown action type and malformed action effective-session payloads | Verified release loading |
| Unauthorized-prior-permit rejection omits rejected-journal disposition proof | Assert exact content-addressed quarantine bytes, active-journal removal, no committed ledger, and a successful subsequent fresh public read | `tests/test_source_selection_and_recovery.py`; `docs/LEDGER_RECOVERY_JOURNAL_LIFECYCLE.md` | unauthorized prior-permit recovery rejection regression | Synthetic recovery mechanics only |
| Alpaca qualification accepts fractional trade counts | Require optional `n` to be an exact non-Boolean nonnegative JSON integer while retaining finite nonnegative numeric validation for `vw` | `providers/alpaca.py`; `tests/test_source_contracts.py` | zero/positive integer controls plus fractional, float-integer, Boolean, string, negative, and non-finite rejection | Provider remains unqualified and network-disabled |
| Frozen executor says absolute return while builder and evaluator preserve economic sign | Name and freeze the exact signed next-open-to-fifth-close simple split-normalized return under registration schema v2; reject internally consistent schema-v1 registrations without migration or relabeling | `research/artifacts.py`; `mechanical_readiness.py`; `config/research_readiness_contract.json`; `docs/HISTORICAL_RESEARCH_HARNESS.md` | positive/negative probability coherence and legacy schema-v1 rejection tests | Synthetic mechanics only; no real-history authority or prior artifact reinterpretation |
| Pending-ledger recovery bypasses the payload validator and append-only uniqueness contract | Bind each typed ledger's validator and unique key at construction so a fresh recovery process can reconstruct them; validate pending and already-committed single/batch journal tails before accepting bytes or cleaning up the journal | `ledger.py`; `monitoring.py`; `trials.py` | invalid payload, duplicate key against history, duplicate key inside an empty-ledger batch, already-committed duplicate tail, and valid recovery tests | Invalid journals are quarantined; recovery never rewrites or blesses an invalid committed tail |
| Repeated inner-fold audit identities silently reweight alpha selection | Enforce one complete cross-fold audit-sample census at the public builder request and retained fit-audit boundaries | `research/builder.py`; `research/artifacts.py` | forged cross-fold duplicate rejected at both request and artifact validation boundaries while generated disjoint folds remain accepted | Synthetic mechanics only; scoring output is unchanged for valid disjoint folds |
| Undersized monitoring evidence can become a non-abstaining warning | Freeze and hash-bind `INVALID -> PAUSED -> PENDING -> WARNING -> OK`: invalid and explicit pause conditions retain precedence, while the minimum window blocks warning or healthy states | `monitoring.py`; `monitoring_policy.py`; `config/prospective_monitoring_policy.json` | undersized warning-threshold observation is persisted and reverified as `MONITORING_PENDING` and abstaining; undersized pause, invalid, adequate warning, and healthy controls retain their states | Policy v1.1 changes the bound hash; legacy hashes fail closed without migration or relabeling |
| Outcome maturation accepts a non-trust-eligible legacy corporate-action ledger | Require exact `actions.trust_eligible is True` at the public release-backed maturation boundary before identity reconciliation or outcome construction | `ledger.py`; `corporate_actions.py` | actual schema-v1 release rejection through `OutcomeLedger.mature_from_releases` plus actual governed schema-v5 gate-through control | Synthetic direct `build_outcome` mechanics remain available; release-backed maturation requires governed coverage |
| Interrupted HFDL capsules are deleted as scratch evidence | While holding the exact build lock, hash and atomically move every failed or stale pending capsule into build-local quarantine, retain it indefinitely with a canonical receipt, and prohibit direct reuse or promotion | `canonical/hfdl_legacy_publisher.py`; `docs/PENDING_RELEASE_LIFECYCLE.md` | partial-materialization and interrupted-retry byte-preservation tests plus successful fresh rebuild | Synthetic legacy-discovery publication only |
| HFDL publisher precheck and public sidecar validation can drift | Use one canonical exact-type and semantic validator in both paths; allow undeclared compatibility fields only as stripped, ignored, untrusted input | `canonical/hfdl.py`; `canonical/hfdl_legacy_publisher.py` | extension compatibility plus Boolean, numeric, container, and semantic parity regressions | Extensions never become trusted or propagated evidence |
| Failed lock acquisition closes its authenticated handle before pathname deletion | On pinned Windows, mark the exact authenticated open handle for deletion before close; never perform post-close pathname unlink, and retain non-Windows failure orphans for explicit non-stealing recovery | `locking.py`; `config/environment.lock.json` | injected durability failure, replacement denial, exact descriptor/path identity, and replacement survival | Windows CPython 3.11 is the pinned supported runtime |
| DSR accepts unbound non-selected trial Sharpes | Require one `ResearchArrayBinding` over the selected returns and complete trial-Sharpe census plus exact sample IDs before any DSR calculation; reject legacy unbound calls | `research/dsr.py`; `research/contracts.py` | non-selected Sharpe tamper, missing binding, sample-census mismatch, and trusted oracle compatibility | Mechanics only; no existing non-test caller or artifact migration |
| Current Alpaca status is backdated to an older Nasdaq file timestamp | Make a merged identity version effective no earlier than the later of Nasdaq source time and the exact Alpaca observation time | `identity.py`; `tests/test_identity_bitemporal_membership.py` | later-observed inactive state remains invisible to an earlier effective-time query and becomes visible at observation time | No schema change; prior serialized evidence is not reinterpreted |
| Migration recovery deletes generic atomic-write names without ownership evidence | Complete a read-only staging census first, preserve and reject every generic `.aw.*.tmp`, and mutate only copy temps bound to the exact plan object, directory, size, and hash | `migration.py`; `tests/test_migration_and_config.py` | unowned atomic temp plus otherwise recoverable copy temp leaves the entire stage byte-for-byte unchanged | Explicit operator disposition is required for unowned failure evidence |
| Runtime environment checks only selected direct distributions | Define the executable project closure as all 22 authenticated pins and compare every normalized installed distribution/version to both matching lockfiles | `environment.py`; `requirements.lock`; `requirements.sha256.lock`; `docs/DEPENDENCY_CLOSURE_POLICY.md` | complete installed-version call census and transitive `six` drift rejection | Unrelated globally installed tooling is outside the project closure |
| Timestamp parsing touches string methods before enforcing its public type boundary | Require exact text before normalization so null, integer, Boolean, list, and object inputs fail with stable `ContractError` | `common.py`; `tests/test_audit_trust_chain_regressions.py` | five non-string type regressions plus existing valid timestamp suite | Valid ISO-8601 behavior unchanged |
| Non-split corporate actions can retain split-ratio semantics | Reject every non-null `ratio_new_for_old` unless the exact action type is `SPLIT`, preserving positive finite split validation | `corporate_actions.py`; `tests/test_calendar_actions_outcomes.py` | complete non-split action matrix with null controls and split compatibility | Malformed historical payloads fail closed when loaded |
| Unfiltered corporate actions can be accepted without asset attribution | Require a nonempty canonical involved-symbol census for every evidence row, including unfiltered requests, without guessing from free text | `providers/corporate_actions.py`; `tests/test_corporate_action_provider_contract.py` | assetless unfiltered provider row rejection plus existing primary/multi-symbol controls | Provider schema changes require separately reviewed recognized fields |
| Outcome diagnostics say every non-split action prevents comparison | Name the actual unsupported corporate-action rule without implying that accepted dividends block price-return outcomes | `outcomes.py`; `tests/test_calendar_actions_outcomes.py` | exact merger diagnostic assertion | Diagnostic-only compatibility fix |
| Outcome construction ambiguously compares bar availability to an action-only clock | Define the schema-v1 timestamp as one conservative action-and-bar evidence cutoff, expose an explicit semantic alias, and test strict-before/equality/strict-after boundaries for both evidence classes | `outcomes.py`; `schemas.py`; `docs/OUTCOME_KNOWLEDGE_CLOCK.md` | bar availability boundary and late-action visibility regressions | Existing serialized field retained; separate clocks require a new schema |
| Path validation is described as continuous hostile namespace confinement | Bound the supported runtime to trusted single-principal cooperative writers, retain link/root checks and exact Windows lock-handle protection, and explicitly require a separate native isolation design for hostile same-account mutation | `locking.py`; `docs/FILESYSTEM_NAMESPACE_THREAT_MODEL.md` | Windows deletion/replacement denial, changed-identity refusal, linked-component rejection, and exact-handle retirement tests | Not an OS sandbox; hostile same-principal ancestor replacement is out of scope |
| Trust-sensitive source tests stop at permissive component mocks | Exercise the real external-signature verifier, request binding, single-use authorization, guarded Alpaca entry point, response capability, atomic landing, detached attestation, verified reload, and tamper refusal with only transport substituted | `tests/test_audit_trust_chain_regressions.py`; `docs/TEST_EXECUTION_EVIDENCE_POLICY.md` | offline guarded-provider integrated trust-chain regression | No provider request or production authority activation; current assessment diagnostics remain external run evidence |
| Release manifest construction silently deduplicates caller paths | Normalize every requested path once, reject duplicate normalized paths before file hashing, and retain deterministic sorting only for a unique caller census | `releases.py`; `tests/test_releases_and_locking.py` | exact duplicate and slash-alias rejection before hashing plus the pre-change unique-input release ID | Public signature unchanged; valid unique manifests remain byte-identical |
| Release publication follows an escaping linked staging ancestor | Authenticate every manifest source against the resolved staging root before pending creation and again immediately before hashing and copying | `releases.py`; `tests/test_releases_and_locking.py` | a separately constructed manifest through a real symlink or Windows junction is rejected before hashing, with no pending or accepted release | Deterministic linked-ancestor escape is closed; hostile concurrent namespace replacement remains outside the documented threat model |
| Manual options selection contaminates scoring | Score every eligible underlying prediction; keep discretionary journal external | Constitution/output schema | coverage reconciliation | Pending milestone 5 |

## Milestone-1 exit rule

All `Milestone 1` rows must have executable synthetic regression coverage.
Current pass status must come from a separately retained run artifact; this
matrix alone is not execution evidence. Pending rows remain explicit blockers
for their later milestone and cannot be promoted by prose or waiver.
