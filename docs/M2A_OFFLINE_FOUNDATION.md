# Milestone 2A Offline Foundation

This is a historical record of the synthetic-only Milestone 2A state. The
later controlled execution superseded its counts and no-copy status without
rewriting or deleting the evidence below.

Milestone 2A implemented and synthetically validated the offline path later
used by the separately executed data-foundation milestone.

## Implemented

- At that milestone, the exact 4,903-file / 345,770,673-byte allowlist was
  enforced by every glob,
  family total, and grand total.
- Copy execution binds an exact reviewed config, inventory, plan, migration-
  code hash, file count, and byte count. It is one-writer, checkpointed,
  resumable, root-contained, link-resistant, source-stability checked,
  collision-safe, and promotes only after an exact no-extras tree verifies.
- HFDL Parquet is paired with its provenance sidecar, hash/schema/OHLCV checked,
  tagged across the 2022-03-04 source break, and permanently labeled
  `LEGACY_DISCOVERY` / non-PIT.
- The synthetic-tested HFDL bridge consumes only the verified two-epoch HFDL
  set plus a pinned accepted XNYS calendar. It emits six physically separate
  causal-bar, feature-input, and outcome-input releases. Epoch pooling is
  prohibited, and exact missing-session, membership, security-type, action,
  delisting, and unevaluable-outcome denominators remain explicit.
- Legacy Alpaca SIP raw gzip pages are reparsed only as failed qualification
  evidence from explicit request/page manifests;
  pagination, provenance, schema, duplicates, OHLCV, identity gaps, and
  deterministic Parquet are checked. Legacy capsule quality remains `FAIL`.
- Active Alpaca qualification makes bounded, separate SIP and IEX requests;
  no active feed or default `asof` is assumed. Responses are byte-bounded and
  post-response receipt times are retained.
- Comprehensive Nasdaq and Alpaca asset snapshots land as immutable
  as-received evidence before conservative bitemporal identity/membership
  parsing. File time is causal, disappearance emits a tombstone, symbol reuse
  cannot inherit eligibility, and unknown types abstain.
- Runtime versions, release quality/epoch/role selection, evaluation chronology,
  sealed inputs, actual-time fit-free inference, canonical prediction IDs,
  external ledger anchors, ordering, and crash recovery fail closed.

## Not executed at Milestone 2A

No legacy payload was copied, no provider was called, no real historical file
was canonicalized into v2, and no model, WFA, or alpha metric ran. No active
configuration points to staging, pending, HFDL, or failed Alpaca evidence.
The bridge has therefore not built a real accepted release and does not make
`HISTORICAL_RESEARCH_READY` true. Because historical identity, membership,
actions, and delistings remain unavailable as received, the bridge emits no
matured return and cannot feed the active research sample schema.

Milestone 2B is the first execution milestone. It must retain the same manifests
and may not weaken a failed source role merely because its bytes reproduce.

## Superseding controlled-execution record

The reviewed successor manifest contained 4,911 files / 345,845,816 bytes and
was hash-copied into a non-active content-addressed release. The accepted
foundation was then built as 11 components with the PiTrading and IEX epochs
kept physically separate. A bounded Nasdaq snapshot was qualified; Alpaca was
not called because credentials were unavailable to this process. None of these
steps ran historical research or changed the non-PIT discovery role.
